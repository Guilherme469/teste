/*const arr = [1, 2, 3];
const arr2 = new Array(1, 2, 3);


/*
const arr = Array.of(1, 2, 3);
*/


/*
const persons = Array.of('John', 'Chris', 'Jenny');
*/

/*
const numbersAndStrings = array.of(1,2, 'strings, 'texto');
*/


/*PUSH = Adiciona um ou mais elementos no final do array 
const frutas = ['melancia', 'banana'];
frutas.push('laranja');
console.log(frutas)



/*POP = Remove o ultimo elemento de um array e retorna o item removido
const arr = ['banana', 'melancia', 'abacate'];
const removedItem = arr.pop();

console.log(removedItem)
console.log(arr);

/*SHIFT

const arr = ['banana', 'melancia', 'abacate'];
const removedItem = arr.shift();

console.log(removedItem);
console.log(arr);

/*CONCAT

const frutas = ['banana', 'melancia']
const salgados = ['coxinha', 'kibe', 'empada']

const alimentos = frutas.concat(salgados)
console.log(alimentos)

/*SLICE = retorna um novo array "fatiando" o array de acordo com inicio e fim

const arr = [1,2,3,4,5];

arr.slice(0, 2);

arr.slice(2);

arr.slice(-1);

arr.slice(-3);

/*SPLICE  Altera um array adicionando novos elementos enquanto remove elementos antigos

const arr = [1,2,3,4,5];

arr.splice(2);

console.log(arr);

arr.splice(0,0, 'first');

console.log(arr);
*/