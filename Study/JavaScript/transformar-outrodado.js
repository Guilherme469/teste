/*
JOIN = Junta todos os elementos de um Array, separados por um demilitador e retorna uma string 

const arr = [1,2,3,4,5];

arr.join('-');

REDUCE = Retorna um novo tipo de dado iterando cada posicao de um array 

const arr = [1,2,3,4,5];

arr.reduce((total, value) => total += value, 0);
*/